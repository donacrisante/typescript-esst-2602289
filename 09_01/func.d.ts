export function calculate(a: number, b: number): number;

// d.ts-Dateien sind Definitioensdateien für Typen. Man benutzt in der Regel unbewusst solche Dateien. 
// Man verwendet d.ts-Dateien, wenn man externe Libraries einbindet. Viele Libraries sind nur in JS geschrieben, und trotzdem will man sie in TS-Projekten verwenden. Und so kann man das Projekt manuell mit d.ts-Dateien ergänzen, und es wirkt so, als wäre es in TS geschrieben.  
